<aside class="socialmediaicons">
	<span> Join the discussions:</span>
	<% appdata.social.forEach(function(item){ %>

		<li> <a href="<%= item.url  %>"><img class="icon" src="<%= item.img %>" alt=" logo for <%= item.shortname %>"> </a>
		</li>
	<% }); %>
</aside>